#pragma once

//extern int obsPosData[16];

//ボール演算のサンプル
/*while (!CheckHitKey(KEY_INPUT_ESCAPE)) {
DrawCircle(sBall.X, sBall.Y, BALLRAD, white);
ScreenFlip();
if (CheckHitKey(KEY_INPUT_SPACE)) {
sVel.Y += gravity;
sBall.X += sVel.X;
sBall.Y += sVel.Y;
}

if (sBall.Y > SCRHEIGHT) {
sVel.Y *= -1;
}
if (sBall.X < 0 || SCRWIDTH < sBall.X) {
sVel.X *= -1;
}

}*/